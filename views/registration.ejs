<%- include('./header'); %>
    <script src="https://www.google.com/recaptcha/api.js?render=6LfZUbcgAAAAAE9GM7z7fYYNO0nsXczoI3cFaJir"></script>

    <section class="form-section">
        <form action="reg" method="post">

            <div class="form-section__inputs">
                <label for="">Электронная почта</label>
                <input type="email" name="email">
                <% if (locals.error) { %>
                    <div class="error-message">
                        <%=error%>
                    </div>
                    <%}%>

                        <label for="">Логин</label>
                        <input type="text" name="login" minlength="2">
                        <label for="">Пароль</label>
                        <input type="password" name="password" autocomplete="off" minlength="8">
                        <div id="captcha">

                        </div>
                        <script>
                            function onClick(e) {
                                e.preventDefault();
                                grecaptcha.ready(function() {
                                    grecaptcha.execute('6LfZUbcgAAAAAE9GM7z7fYYNO0nsXczoI3cFaJir', {
                                        action: 'submit'
                                    }).then(function(token) {
                                        console.log(1)
                                    });
                                });
                            }
                        </script>
                        <input type="submit" value="Регистрация">
            </div>
            <a href="/login">Войти</a>
        </form>
    </section>
    <%- include('./footer'); %>